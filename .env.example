# 复制为 .env 并填入真实 key（.env 已加入 .gitignore，不会被提交）
DEEPSEEK_API_KEY=your-api-key-here

# 以下均为可选，有默认值
# DEEPSEEK_MODEL=deepseek-v4-pro
# AGENT_MAX_ITERATIONS=20
# AGENT_MAX_TOOL_OUTPUT_CHARS=4000
# AGENT_MAX_HISTORY_MESSAGES=40
# AGENT_COMMAND_TIMEOUT=30
# AGENT_WORKSPACE=/path/to/your/workspace
